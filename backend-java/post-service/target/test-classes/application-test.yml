# Application properties for testing
spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb
  
supabase:
  url: http://localhost:54321
  key: test-key
  service-role-key: test-service-key
  storage:
    bucket: test-bucket
    folders:
      temp: temp
      posts: posts
      avatars: avatars

firebase:
  project-id: test-project
  credentials:
    path: classpath:firebase-credentials-test.json

pycuda:
  service:
    url: http://localhost:5000
    timeout: 5000
